{
  "name": "matt-newman-cv",
  "version": "0.1.0",
  "devDependencies": {
  },
  "dependencies": {
  },
  "scripts": {
    "build": "JEKYLL_ENV=production jekyll build",
    "dev": "jekyll serve --livereload",
    "server": "jekyll serve"
  },
  "test": "echo 'no test setup'"
}
